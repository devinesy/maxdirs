{
  "name": "maxdirs",
  "version": "0.0.8",
  "author": "Paul Devine [:devinesy]",
  "description": "Utility to trim the directories in a directory based on a maximum allowed, deletes oldest first",
  "keywords": [
    "directory",
    "purge"
  ],

  "main": "lib/maxdirs.js",
  "bin": {
    "maxdirs": "./bin/maxdirs"
  },
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "rimraf": "~2.2.6"
  },
  "devDependencies": {
    "nodeunit": "~0.8.6"
  },
  "scripts": {
    "test": "nodeunit test"
  },

  "repository" :
  {
    "type" : "git",
    "url" : "http://github.com/devinesy/maxdirs.git"
  }
}
